angular.module('viewCustomerOrderApp', ['smoothScroll'])
        .controller('viewCustomerOrderController', function ($scope, $rootScope, $timeout, smoothScroll) {

            /* Function will be executed after the page is loaded */
            $scope.$on('$viewContentLoaded', function () {                
            });
            
        }); 